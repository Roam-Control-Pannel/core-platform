// Flat ESLint config for @roam/console — consumes the shared next preset
// (base TS rules + react-hooks + @next/eslint-plugin-next). One source of
// lint truth; this file only points at it. Replaces the deprecated next lint.
import next from "@roam/eslint-config/next";

export default next;
