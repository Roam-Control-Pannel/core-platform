# Expo/React Native needs a hoisted (flat) node_modules — its native module
# resolution and expo-doctor assume npm/yarn-style layout, not pnpm's strict
# symlinked store. Scoped to this app only; the rest of the monorepo keeps
# pnpm's default strict linking. This is Expo's documented monorepo guidance.
node-linker=hoisted
