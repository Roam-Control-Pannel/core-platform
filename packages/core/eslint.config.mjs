// Flat ESLint config for @roam/core — consumes the shared base preset.
// One source of lint truth (@roam/eslint-config); this file only points at it.
import base from "@roam/eslint-config/base";

export default base;
